Easier since it's a search tree

root is bigger than all left elements
smaller than all right elements

if root is between p and q, 
    then root is the LCA
else if root is bigger
    check left subtree
else if root is smaller
    check right subtree
